let messageFunction = (message) => console.log(message)

messageFunction("Hello World")

//to convert this ts file to a js file, execute the below command in cmd
//tsc <fileName.ts>
//this will create a next .js file.